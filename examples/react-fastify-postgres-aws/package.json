{
  "name": "visual-parity-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "capture:production": "tsx ../../scripts/visual-parity/capture.ts visual-parity.production.config.json",
    "capture:local": "tsx ../../scripts/visual-parity/capture.ts visual-parity.local.config.json",
    "compare": "tsx ../../scripts/visual-parity/compare.ts visual-compare.config.json",
    "test:visual": "npm run capture:production && npm run capture:local && npm run compare",
    "clean": "rm -rf visual-tests"
  },
  "devDependencies": {
    "playwright": "^1.40.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0",
    "sharp": "^0.33.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.10.0",
    "@types/pixelmatch": "^5.2.6",
    "@types/pngjs": "^6.0.5"
  }
}
